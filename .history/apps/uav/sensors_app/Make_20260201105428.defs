############################################################################
# apps/uav/sensors_app/Make.defs
#
# Sensors App Make Definitions
#
############################################################################

ifneq ($(CONFIG_UAV_SENSORS),)
CONFIGURED_APPS += $(APPDIR)/uav/sensors_app
endif
