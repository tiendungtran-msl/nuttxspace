#
# For a description of the syntax of this configuration file,
# see the file kconfig-language.txt in the NuttX tools repository.
#

config EXAMPLES_LEDTIMER
	tristate "Led timer example"
	default n
	depends on TIMER
	---help---
		Enable the Led timer example

if EXAMPLES_LEDTIMER

config EXAMPLES_LEDTIMER_STACKSIZE
	int "Ledtimer stack size"
	default DEFAULT_TASK_STACKSIZE
	---help---
		This is the stack size allocated when the timer task runs

config EXAMPLES_LEDTIMER_PRIORITY
	int "Ledtimer task priority"
	default 100
	---help---
		This is the priority of the timer task

config EXAMPLES_LEDTIMER_PROGNAME
	string "Timer program name"
	default "timer"
	---help---
		This is the name of the program that will be used when the NSH ELF
		program is installed.

endif
