#
# For a description of the syntax of this configuration file,
# see the file kconfig-language.txt in the NuttX tools repository.
#

config EXAMPLE_MAKE_PW_PROGNAME
    tristate "GPIO_v2 driver example"
    default n
    depends on DEV_GPIO
    ---help---
        Enable the GPIO driver example

if EXAMPLE_MAKE_PW_PROGNAME

config EXAMPLE_MAKE_PW_PROGNAME_PROGNAME
    string "Program name"
    default "gpio_v2"
    ---help---
        This is the name of the program that will be used when the NSH ELF
        program is installed.

config EXAMPLE_MAKE_PW_PROGNAME_PRIORITY
    int "GPIO task priority"
    default 100

config EXAMPLE_MAKE_PW_PROGNAME_STACKSIZE
    int "GPIO stack size"
    default DEFAULT_TASK_STACKSIZE

endif