############################################################################
# boards/arm/stm32h7/weact-stm32h743/src/CMakeLists. txt
############################################################################

set(SRCS
    stm32_boot.c
    stm32_bringup.c
    stm32_gpio.c
    stm32_appinitialize.c
    stm32_spi. c)

if(CONFIG_USERLED)
  list(APPEND SRCS stm32_userleds.c)
endif()

if(CONFIG_STM32H7_SDMMC1)
  list(APPEND SRCS stm32_sdmmc.c)
endif()

if(CONFIG_FAT_DMAMEMORY)
  list(APPEND SRCS stm32_dma_alloc.c)
endif()

# Add other source files as needed (USART, I2C, ioctl, etc.)

target_sources(board PRIVATE ${SRCS})